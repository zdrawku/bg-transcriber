# Path to the folder containing your audio files (M4A, MP3, MP4, WAV, etc.)
INPUT_FOLDER=D:\folder\folder

# Whisper model size: tiny, base, small, medium, large
# Larger models are more accurate but slower
WHISPER_MODEL=large
